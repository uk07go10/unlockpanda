- var base = '/catalog/view/theme/ur/'
doctype html
html.no-js(lang='en')
    head
        | <?= $scripts ?>
    body
        | <?= isset($header) ? $header : "" ?>
        | <?= isset($header_form) ? $header_form : "" ?>
        section#page-content.page-wrapper.mt-50
            // SHOP SECTION START
            .shop-section.mb-80
                .container
                    .row
                        .col-md-12
                            h3 <?= $heading_title; ?>
                    .row
                        | <? foreach($products as $product): ?>
                        .col-xl-3.col-lg-3.col-md-3.col-sm-4.col-xs-12.text-center
                            a(href!="<?= $product['href']; ?>")
                                img.img-responsive.center-block.mb-10(src!="<?= $product['thumb']; ?>", alt!="<?= $product['name']; ?>", title!="<?= $product['name']; ?>")
                                h5 <?= $product['name']; ?>
                        | <? endforeach; ?>

                    .row.mt-20
                        .col-md-12.text-center <?= $pagination; ?>

            | <?= $footer ?>  
        